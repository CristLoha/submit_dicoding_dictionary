import 'package:flutter/material.dart';

class WebBookmark extends StatelessWidget {
  const WebBookmark({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
